.list-group
    if categories.length > 0
        each category in categories
            a(href=`/categories/${category.id}` style="text-transform: capitalize;" class=category.id===Number(selectedCategory) ? "list-group-item active" : "list-group-item" ) #{category.name} 